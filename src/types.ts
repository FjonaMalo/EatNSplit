export interface FriendProps {
  friend: {
    id: string;
    name: string;
    image: string;
    balance: number;
  };
}

export interface FriendTypes {
  id: string;
  name: string;
  image: string;
  balance: number;
}
